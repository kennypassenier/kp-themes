# The look-round: five measurements waiting on Kenny

Five rows in `docs/MINI_ROUNDS.md` name their moment as "the next time
Kenny looks at it". They are not approvals — nothing is signed off — and
together they take about five minutes. This page puts them in one place
with, per row, where to go, what to see, and what a good answer is.

Opened 2026-09-20, after Kenny chose to work the queue and answered
**Claude zet de kijklijst klaar** on the form of the same evening.

| #   | What to look at                                                         | Which row it closes                                                             |
| --- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| 1   | `catalogue/table.html#datatable-sticky`, wheel-scrolled in three themes | row on row text above a sticky header                                           |
| 2   | `catalogue/overlays.html#confirm` in dark                               | the last quarter of the row on the chamfered corner                             |
| 3   | `catalogue/field.html#multiline` in dark and titanium                   | the row on a select showing two arrows                                          |
| 4   | `catalogue/round.html`                                                  | the row on the round page answering its own question                            |
| 5   | `catalogue/index.html`                                                  | the rows on a round staying finished and the site opening with nothing to judge |

Two of the five are about what a browser other than the test browser
shows, which is why the page asks for FireDragon: the tests run in
Playwright's Firefox, and that is a different build.

This page carries no `.cat-block`, so it adds nothing to the verdict
register and nothing to judge.
