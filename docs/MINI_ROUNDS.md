# Mini-round and measurement queue — kp-themes

Open items that must not live only in a conversation: pending
measurements from correction forms, and mini-rounds that reopen a frozen
decision. An entry leaves this list only when it has actually happened,
with the result recorded.

| ID  | What | Opened | Triggers at | Status |
| --- | ---- | ------ | ----------- | ------ |
| KT1-M1 | Count, in a decision form, how many claims assert a fact about code and how many carry a file:line or an explicit second-hand label. **Taken at the Phase 7 gate form, 2026-09-04: nine claims, nine sourced.** They matched. Kenny's answer at H3 was that one matching count does not undo a measure already broken once, so KT1's fallback applies from here regardless. | 2026-09-03 | done at the Phase 7 gate | **closed** |
| KT1-M2 | Whether KT1 field 8's fallback triggers, after the measure failed once following its own approval. **Decided 2026-09-04 at the Phase 7 gate: it triggers.** From now on every claim in a form for this project carries its source, or becomes its own item in that form. Recorded in `docs/CORRECTIONS.md` and in the project rule in `CLAUDE.md`. | 2026-09-03 | done at the Phase 7 gate | **closed** |
| F1 | **Nothing in this package styled a link.** Closed by L6 on 2026-09-04: `css/_rules.css` now styles `a`, `a:visited` and `::selection`, `--link` and `--link-visited` are derived per theme, four contrast pairs gate them, and a browser test measures the painted link colour against the painted background on all seven fixtures. The gate no longer verifies a promise the package does not keep. | 2026-09-03 | closed at L6 | **closed** |
| MR-NPM | Mini-round of 2026-09-04: Kenny dropped JobTracker's build step as a supported environment and decided this package publishes nothing to npm at all. Outcome, all approved: S19 added (the package does not prescribe how it is consumed); two constraints struck from S8 (`allow-git`, the Tailwind `@source`); S10 rewritten to promise only what this package can prove; M1's Dependabot measurement closed unmeasured because the mechanism it watched no longer exists. Recorded in `docs/SCOPE.md` and `docs/FEATURES.md` with dated amendment notes. | 2026-09-04 | — | closed |
| MR5-PAGES | The showcase is published on GitHub Pages, and Claude enabled that setting. Kenny approved it in the AFK report of 2026-09-04. The site is assembled by `.github/workflows/pages.yml` from `showcase/` plus the three directories it loads, so `docs/` is not published. Live at https://kennypassenier.github.io/kp-themes/ | 2026-09-04 | done | **closed** |
| KT2 | Correction KT2: a gate checked one half of a two-halved property. Measurement of field 7 done in the same session — 9 violations before, 0 after, all 28 interactive states at 10.1 to 12.1, and the two tests that pinned the fault rewritten into their opposite. | 2026-09-04 | done at the repairing commit | **closed** |
| AR8-D1 | Defect found by the Phase 4 critic pass and verified by hand on 2026-09-04: `scripts/check-contrast.mjs` discovers themes with a pattern matching lowercase letters only, so a theme named `high-contrast` or `topo2` is silently skipped while the run reports every theme passing; the floor test only fires below five themes, so with seven present an eighth goes unnoticed. **Closed 2026-09-04 in L1.** Three tests were written first and failed against the broken version; the pattern now accepts digits and hyphens, and the theme count is checked against `themes/order.json` instead of a floor of five. The gate moved to `gates/check-contrast.mjs` per S18. | 2026-09-04 | done in L1 | closed |
| step-3 | Correction KT16, field 7 (this row was `KT16-M1` until it was rewritten; see `docs/ID_TRANSLATIONS.md`): at the next full `npm run verify` Kenny gives, the suite is green and no test reads computed style after an action without a second chance. The 63 sites that did were converted on 2026-09-09; this closes when a full run has passed with them in place. **Measured on 2026-09-10 and it did not hold:** Kenny's verify ran `gates 0:12 ok`, `browser 7:14 FAILED`, on two tests neither of which was among the 63 — one in a spec that is not a register spec, one reading after a page load rather than after an input. The measure was written for the half of the fault it was found in. Recorded as `fix-1`, whose own measurement replaces this one. | 2026-09-09 | at Kenny's next full verify run | **closed** |
| step-2 | Correction `fix-1`, field 7: at Kenny's next full `npm run verify` the browser suite is green with the seven repaired sites in place — four focus-ring readings that now read until the ring is whole, three animation readings that now ask a record armed before the page existed. This closes only when a full run has passed; the run Claude made on 2026-09-10 counts as evidence, not as the measurement, because the fault this exists about is one that only appears under someone else's load. **Evidence on 2026-09-10:** the release verify was green — gates 0:12, browser 6:02, advice 0:01, total 6:15 — but it ran on this machine, with Kenny's go rather than his keyboard. That is exactly the case this row said would not close it, so it stays open rather than being resolved in the direction of the one who wrote it. | 2026-09-10 | at a full verify run on Kenny's own machine | open |
| step-4 | Correction `fix-2`, field 7: no bare playwright run in the transcript that Kenny did not clear. Two moments close it — the next change that needs a drill (does Claude ask for the second engine instead of taking it?) and the next release (does Claude ask for the suite in a form, and only then run it?). **The release half is met (2026-09-10):** Claude asked on the `verify-go` form, Kenny gave the go, and the run followed — no unasked suite run in that chain. The drill half is still ahead. | 2026-09-10 | at the next drill | open |
| step-5 | Correction `fix-3`, field 7: the next tag's release job gets past its gates and builds its assets. It closes on a green Release run, not on a green local one — that is the whole point of the fault. **Closed on 2026-09-10:** the re-tagged `v5.1.0` ran green in 36 seconds where the first attempt died after 28, and built all nine assets including `consumer.tar`. | 2026-09-10 | closed at the `v5.1.0` release run | **closed** |
| gap-1 | **The destructive alert's text is unreadable in many themes [Kenny, 2026-09-10].** Seen while looking at chassis-rs running 5.1.0: `<div class="kp-alert kp-alert--destructive" role="alert">` with a sentence in it reads as white on white in a lot of the twenty-five, so the message is there and cannot be read. Recorded as HIS observation and not measured here — his instruction was to write it down and touch nothing. What that measurement would be, when it is asked for: `--destructive-foreground` against the plate the alert actually paints, per theme, in the browser rather than from the tokens, because the alert's own rules may not be using the pair the contrast gate checks. Note for whoever picks it up: the accessibility floors have been advice rather than gates since 2026-09-09, so a failing pair here would have been printed and not refused. **Answered on the round-seven scope form, 2026-09-11: it comes along.** Kenny chose "Alle vier mee", so this is built in round seven rather than shifting again (`scope-6` in docs/SCOPE.md). | 2026-09-10 | at the next version | open |
| gap-2 | **The theme picker moves along the top bar from theme to theme [Kenny, 2026-09-10].** Same sitting: the control does not sit in the same place across themes, because themes size things differently and the bar's contents shift with them. Recorded as his observation, not measured here. The question it raises is a design one rather than a bug: a control a user reaches for by muscle memory arguably belongs at a fixed place regardless of a theme's spacing scale, and that would be a constraint on every register rather than a fix in one. **Answered on the round-seven scope form, 2026-09-11: it comes along.** Kenny chose "Alle vier mee", so this is built in round seven rather than shifting again (`scope-6` in docs/SCOPE.md). | 2026-09-10 | at the next version | open |
| gap-3 | **Synthwave's stripe restarts on every cell, so a table header shows a seam per column [Kenny, 2026-09-10].** His words: the gradients are beautiful, but in a table each `<th>` carries its own, and where two of them meet the end of one gradient butts against the start of the next. Recorded as his observation and not changed — his instruction was to remember it for the next round. Located, because a row that names the rule is worth more later than one that names the symptom: `css/synthwave-register.css:1019`, `[data-theme='synthwave'] .kp-table th` carrying `border-bottom: 2px solid transparent; border-image: var(--kp-stripe) 1`, where `--kp-stripe` is `linear-gradient(to right, var(--primary), var(--accent))`. A border-image is painted per element by definition, so each header restarts the ramp — the effect he saw is the mechanism working as written rather than a mistake in the value. What he asks for is one gradient across the whole row, which means painting it once on something that spans the row (the `thead`, or a pseudo-element on it) instead of on each cell. Searched rather than assumed: `.kp-table th` carries a `border-image` in synthwave and in none of the other twenty-four registers, so this is one theme's problem. The same declaration does sit on six other synthwave elements — the nav menu, the form summary, the card, the theme menu, the wizard and the spec list — and none of those repeat side by side, which is why only the table shows it. **Answered on the round-seven scope form, 2026-09-11: it comes along.** Kenny chose "Alle vier mee", so this is built in round seven rather than shifting again (`scope-6` in docs/SCOPE.md). | 2026-09-10 | at the next round | open |
| step-6 | Correction `scope-7`, field 7: the compliance table reads pass on the flash threshold once the rating function is called with its cycle count, and a test lays the table's verdict next to the gate's so the same sum in two places cannot drift again. Measured both ways here on 2026-09-11 before the form was built: thirty-six animations rated, fourteen over the threshold on the two-argument call and none on the three-argument one, all fourteen with exactly one cycle. Kenny answered Klopt on the scope form and started the repair in a separate session in this same working tree; that work is a claim here until this session has seen it land, per the rule that another session's change is verified rather than believed. | 2026-09-11 | at the first regeneration of the compliance table | open |
| gap-6 | **The form widget needed scrolling to hit a control [Kenny, 2026-09-11].** On the element form he had to scroll to find the exact position before a pill or an input would take a click. Not measured here, because the widget is not this project's code and the behaviour did not reproduce in any surface Claude can drive. What is this project's doing, and is the first thing to change: the choice cards on that form carried `min-width:190px` with labels long enough to push a row of five past the width of the panel, which is the shape most likely to produce exactly that symptom. The next form drops the minimum width and lets the cards wrap, and Kenny is asked in that same form whether it behaved. | 2026-09-11 | at Kenny's next form | open |
| gap-7 | **Ten registers cut no silhouette at all [measured 2026-09-11].** Counted across the twenty-five registers: cyberpunk reaches for `clip-path` thirty-six times, phantom and solstice ten, retro eight, and ten registers use it zero times — academia, brutalism, dark, formal, grotesk, mono, sepia, shade-dark, terminal and ticker. On the button rule specifically, only blueprint declares a polygon; every other theme's button is a plain rectangle or the default radius. The row exists because the obvious conclusion is wrong and worth writing down before someone acts on it: the count does not predict Kenny's verdict, since terminal and brutalism cut nothing and he calls both very good. A quirk is behaviour, not a shape budget. | 2026-09-11 | at the quirk pass of round seven | open |
| gap-5 | **The concept demo was a major behind the package on destructive actions [Kenny, 2026-09-11].** He spotted it on the first cut of the hypertech candidates: the wipe-form button armed itself and waited, which is the 3.x confirmation, while `js/components.js:274` has defaulted `attachConfirmations` to `mode = 'dialog'` since 3.0.0 and builds a real `.kp-dialog.kp-confirm` with a title, a description and two actions. His instruction: fix it for the future. Fixed in the candidate page the same day, in the package's own markup and class names, with the theme styling the dialog. What stays open is the wider question his remark implies and this row exists to carry: the inventory in `showcase/concept-demo.json` says only `data-kp-confirm`, so a demo can satisfy every marker and still show a pattern the package retired — the marker records that an obstacle exists, never which one. Worth deciding at the point the inventory is rewritten for hypertech, which `scope-9` will force anyway. | 2026-09-11 | at the rewrite of the concept-demo inventory for hypertech | open |
| gap-4 | **A button hover is a colour swap in half the themes [Kenny, 2026-09-11].** His words, while round seven was being proposed: a hover on a button is not a simple colour change — a bracket may slide around the label, a progress sliver may fill along the button's edge, a specific data label may light up; it should feel like physical feedback from a console. He added that the package does part of this already but probably not enough, across all themes. Measured here rather than assumed, per register and per token: of the twenty-five registers, eighteen declare nothing but colour properties on a `.kp-button…:hover` rule, and six use a pseudo-element at all. Crossed with `--fx-lift`, which the shared base rule in `css/components.css:500-505` turns into a translate, twelve themes have a button hover that is a background colour and nothing else — academia, blueprint, deco, formal, grotesk, mono, sepia, shade-dark, shade-light, terminal, ticker and woodblock, all of them at `--fx-lift: 0px`. The seven that do more are the loud ones: brutalism (font-weight, letter-spacing, text-transform, border-radius, box-shadow, translate), phantom (box-shadow, width, filter, transform), synthwave and cyberpunk (box-shadow, transform, animation), forest (transform, box-shadow, background-position), retro (box-shadow) and high-contrast (transform). This is the specification for the `react` gesture if the motion vocabulary is adopted, and it is Kenny's own requirement rather than a proposal. **Answered 2026-09-11: this is the specification for the `react` gesture** (`scope-2`), not a proposal of Claude's. The three concept demos each answer it differently — brackets closing round the label with a spectral sliver filling the edge, four corner ticks snapping in with a data label lighting above, and glass lifting with a sweep crossing it. | 2026-09-11 | at the round-seven scope form | open |
| CP1 | **The arrival swallows every click but Skip.** Four themes have an arrival — phantom (`card`), retro, terminal and synthwave (`boot`) — and all four build the same overlay: `position: fixed; inset: 0; z-index: 100`, doorlaatbaar pas met `is-off`. Measured 2026-09-09 in Chromium: it eats clicks for 0-1100 ms (phantom), 0-1000 ms (terminal, synthwave) and 0-600 ms (retro), and only `.kp-boot__skip` ends it — a click anywhere else on the overlay does nothing and gives no sign the click was lost. Found because JobTracker reported "the theme picker does not work on phantom"; the picker itself is fine, measured over five switches in both engines. Kenny's instruction on 2026-09-09: **remember it for the next version**, do not act now. The proposal on the table is to hang the existing Skip handler on the whole overlay. | 2026-09-09 | at the next version after 5.1.0 | open |
| ask-1 | **`.kp-badge` cuts a short word in half [chassis-rs, 2026-09-10].** The http-switchboard 3.0.0 browser drill found the state badge on the clients page rendering "acti / ve" over two lines, Formal theme at a 1000 px viewport; a withdrawn client escapes it because a date follows the word and the badge is then wider than its content. Measured here in `css/components.css:428`: `.kp-badge` shares one rule with `.kp-button`, `.kp-tag`, `.kp-health` and `.kp-copyable` — `max-inline-size: 100%; overflow-wrap: anywhere` — and the comment above it records why the rule exists at all: at 360 px those components held one unbroken value at full width and pushed the document sideways, `.kp-badge` measured at 485 px. `anywhere` was chosen over `break-word` deliberately, because only `anywhere` lowers the min-content contribution a flex item's automatic minimum size is measured against. So both cases are real and chassis-rs's premise — a badge holds a label, never a value — is the half this package measured against. The request is not urgent: chassis-rs keeps one line in its own stylesheet, pointing at this entry, and removes it when the component answers. **Answered by Kenny on 2026-09-10: a knob.** `.kp-badge` now reads `overflow-wrap: var(--kp-badge-wrap, anywhere)`, so the default and every existing page are untouched and a consumer whose badges only hold labels sets `--kp-badge-wrap: normal` — on the column rather than on the badge, because the column is what knows. A custom property rather than a modifier class for the same reason. Two cases in `tests/fixtures/narrow.html` and one test in `tests/overflow-floor.spec.mjs` measure it by the painted height, not by the declared value; the drill removed the declaration and the test went red in both browsers while the five floor tests stayed green. Documented in `docs/USER_GUIDE.md` and in the changelog, and the knob count in `gates/site/extractors.test.mjs` moved from 86 to 87. chassis-rs has been told it can drop its own line. **Answered 2026-09-11: repaired first, before anything else in the round.** Twenty-one themes are getting an arrival (`scope-3`), and they would all inherit this overlay, so the repair goes in front of them. The proposal on the table — hanging the existing Skip handler on the whole overlay — was built into the concept demo and measured there: a click that is not Skip ends the overlay. | 2026-09-10 | closed at Kenny's answer | **closed** |
| step-1 | **chassis-rs waits for the `v5.1.0` tag [2026-09-10].** It vendors this package per tag, with a gate that checks every vendored file against the release, so fetching before the tag exists breaks that gate. It therefore stays on 5.0.0 with its own one-line badge override and has written down what it does once the tag lands: move `kp_themes` to 5.1.0, re-vendor, replace its own rule with `--kp-badge-wrap: normal` on the status column, and keep the test that pins the cell. The obligation on this side is one message: tell chassis-rs when the tag is pushed, which is a thing a conversation forgets and a queue does not. **Done on 2026-09-10**, after `v5.1.0` was published with its assets: the message names the badge knob and where to set it, the tarball and its verification command, the three-way checksum, and the fact that the tag briefly pointed elsewhere before any release existed. | 2026-09-10 | closed at the publication of `v5.1.0` | **closed** |
| feat-ha-1 | **Two Home Assistant registers, from a proof to the generator [Kenny, 2026-09-09, approved 2026-09-10].** The generated HA themes carry 23 variables, the card radius and width, and the theme's own duration as a transition on `ha-card` — the colours travel, the expression does not: a register is 28-52 kB of CSS against `.kp-*` classes a dashboard has never heard of. card-mod v4.2.1 is installed on Kenny's instance, so arbitrary CSS per theme is possible. He asked for two proofs, cyberpunk and synthwave, written by hand onto the instance on 2026-09-09 while the repository's generated copies stayed unchanged. Claude could not look at them: no screenshot engine is installed, so Kenny's own look at his dashboard was the only verification available, and on 2026-09-10 he approved them. Both blocks now live in a `REGISTERS` map in `gates/generate-ha-themes.mjs`, and every colour and face in them comes from the same tokens as the rest of the file — the hand-written originals spelled their values out, the generated ones cannot drift from the theme. `ha/kp-cyberpunk.yaml` grew from 1,813 to 3,347 bytes and `ha/kp-synthwave.yaml` from 1,847 to 3,707; the other twenty-three keep colour and timing only, which is a decision rather than an unfinished list. Both files were written back to the instance byte-for-byte and the themes reloaded, so the instance and the repository agree again. | 2026-09-09 | closed at Kenny's approval | **closed** |
| HA4 | **The Home Assistant frontend freezes on a tab change [Kenny, 2026-09-09].** He sees the UI block on the control-panel dashboard, mostly when switching views, with Firedragon offering to close the page, and it recovers after a few seconds. Nothing about it is in Home Assistant's own log: the only errors there were the four `Theme <Dutch name> not found` failures from the stale picker, which are fixed. Two candidates, and one measurement separates them. Since 2026-09-09 cyberpunk and synthwave carry a hand-written `card-mod-card` block, which card-mod applies per card and re-applies when the view changes; but card-mod was already active before that, because `gates/generate-ha-themes.mjs` has written a `card-mod-root` block into every theme since 2026-09-04, and the dashboard is 49 kB over nine views. Kenny chose **Eerst meten**: he opens the dashboard on Sepia (no hand-written block) and on Cyberpunk (one) and reports whether it freezes in both. Claude touches no CSS until then. **Not reproduced (2026-09-09):** after the picker was repaired Kenny reported "de themes werken goed, voorlopig krijg ik geen haperingen", so the Sepia/Cyberpunk comparison has no freeze to compare. One hypothesis is worth keeping and is not a conclusion: while the picker was stale, every selection called `frontend.set_theme` with a name no theme had, which the system log shows failing four times, and the clicking that produced those four errors is the same sitting in which the freezes were seen. The item stays open — an intermittent symptom that stopped is not a symptom that is explained. | 2026-09-09 | at the freeze recurring, or at a second quiet stretch | open |
| HA5 | **A kp-card for Lovelace [Kenny, 2026-09-09].** He asked whether a frontend of his own could replace HA's cards, and chose the smallest of the three routes: a custom card. `www/kp-card.js` (13,436 bytes) is on the instance and registered as the module resource `/local/kp-card.js`, and one example card sits on the Weergave view. It renders inside a shadow root and adopts `fonts.css`, `themes.css`, `components.css` and the theme's register, fetched from the v5.0.0 tag over jsDelivr; `js/effects.js` is attached to the card's wrapper with `manageRoot: false`. Verified in a browser harness on 2026-09-09: the decipher headline resolves, the marquee builds its doubled track, a `<mark>` clears, and nothing leaks — Home Assistant's `<html>` carries no kp tokens and its `body::after` texture layer is absent. **Answered by Kenny on 2026-09-09**: the card is approved as it stands (HA6 "Klopt"), and its source stays on the instance only (HA7 "Alleen op de instance") — Home Assistant's own config backup is the single copy, there is no repository for that config on this machine, and S20 is left intact rather than stretched. The example card and the resource stay. | 2026-09-09 | closed at Kenny's approval | **closed** |
| PROC-H1 | Defect in the shared procedure repository, found on first use 2026-09-04: `hooks/check-commit.sh` decides a command is a commit by testing whether the command string contains the words, so any command whose text merely quotes an example message is blocked. Writing `docs/REALIZATION_PLAN.md` triggered it, because that document quotes one. It fails closed, so it is noise rather than a hole, and the git-native layer is unaffected because it inspects the real message. Belongs to `~/Projects/dev-procedure` and affects every project that installed the hook; raise it there rather than patching a local copy. **Two more facets, both hit on 2026-09-04.** (1) The hook gates the tree as it stands *before* the command runs, so a compound command that formats and then commits is always judged on the unformatted tree and can never succeed. (2) Worse: it blocks the whole command, so the setup steps in it — creating a branch, for instance — never run either, and the retry then lands the work on whatever branch was already checked out. That happened here: an L2 commit intended for a feature branch landed on `main` and had to be moved. The workaround is discipline, not code: never put setup and a commit in one command. **Closed on 2026-09-11**, at the moment this row named — and it turns out it was repaired at the round-five retrospective and simply never ticked off here. Verified against THIS project's own copy rather than against the canonical file, because the canonical one being right proves the source works and not that the project has it: `python3 ~/Projects/dev-procedure/hooks/check-commit.test.py ~/Projects/kp-themes/.claude/hooks/check-commit.sh` gives 17/17, including three drills named after the three facets. Facet 1 is repaired: heredoc bodies and quoted strings are masked before anything is matched and the match must sit at a command position, so a document quoting a commit message is no longer a commit. Facets 2 and 3 became a stated contract instead of a repair — the hook still judges the tree before the command runs and still blocks the whole command, but it now says so in its own message and lists what may precede a commit. That message was read here on 2026-09-10 when it refused `wc -c … && git commit`, and it named the remedy in the same breath. | 2026-09-04 | closed at the round-six retrospective | **closed** |
| DI4-SCOPE | How strict the colour-deficiency check on the status badges must be. Decided 2026-09-04: the gate keeps measuring the one pair where confusion is harmful (offer against rejected), which all seven themes clear. Widening to all 21 pairs would delete six pale palettes rather than tighten a check — 13 to 16 pairs fall under the floor in five themes, 2 in cyberpunk, because cyberpunk is saturated and the others are pale by design. Recorded with the full measurements in `docs/DESIGN_INVARIANTS.md` under DI4. | 2026-09-04 | done | **closed** |
| L3-EXIT | L3's exit criterion said the compliance table reads pass everywhere, which L3 could not reach alone. Decided 2026-09-04: clean up and enforce. DI9 was the last ungated row; measuring it found 42 colour literals duplicating tokens, three of them already drifted. All 42 now read from tokens, `gates/check-layers.mjs` keeps it that way and has been red once, and the compliance table has no ungated rows left. Nothing on screen changed — the largest shift measures 0.37 after compositing. | 2026-09-04 | done | **closed** |
| KT3-M1 | Correction KT3, field 7, part (c). **Measured 2026-09-04, and the honest result is that the discipline was not applied while round two was being built:** roughly sixty browser tests were written without a recorded drill. Performed afterwards on the two most at risk — the DataTable's card layout (delete the `display: flex` rule, both channels go red) and the combobox's virtual focus (delete the `aria-activedescendant` line from js/listbox.js, the framework-free case goes red while React correctly stays green, because the two channels are two implementations). Both drills are now recorded in the tests themselves. What this measurement actually showed is that the rule gets skipped under AFK volume, which is what field 8 has to answer. Part (b) was measured at the moment of building: the pre-fix `body { font-family }` put back into `showcase.css` makes `gates/check-layers.mjs` exit 1 naming line 9, and 0 once restored. | 2026-09-04 | done during round two, late | **closed, with a finding** |
| KT4-M1 | Correction KT4, field 7: JobTracker's own typecheck against the shipped declarations. **Measured by the JobTracker session, 2026-09-05, on 2.0.0: passed, and stronger than asked** — `tsc -p jsconfig.json` clean, then again with `--skipLibCheck false` because their config skips library checks by default: clean, zero errors, none in `@kp-soft/themes`. Their config: strict, NodeNext, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `checkJs`. The declarations hold up on their own rather than being skipped. | 2026-09-04 | JobTracker's adoption of 1.1.0 | **closed** |
| TH47 | The visual filter builder was the one candidate Kenny could not rate from the description: "kan je hier meer info over geven, met voorbeelden en misschien een link naar een live voorbeeld ergens? want ik snap niet wat je bedoelt" (2026-09-04). Answered with the Notion and Airtable equivalents and a live demo; awaiting his rating. | 2026-09-04 **Closed in fact on the same day, and this row was stale until 2026-09-07.** `docs/FEATURES.md:143` records the rating: **Later**, given 2026-09-04 once the description named the Notion and Airtable equivalents and pointed at a live demo, with the reasoning that it shares its whole mechanism with TH37's filtering and is superstructure rather than a second system. Found while writing round five's Phase 0 form, which claimed on this row's authority that TH47 was still unrated — it was not. | 2026-09-04 | done at the round-two rating form | **closed** |
| PROC-H2 | Fourth facet of PROC-H1, found 2026-09-04 while building round two: the traceability half of `hooks/check-commit.sh` looks for bracketed feature IDs in the COMMAND text, so a message passed with `-F file` (or any other way of keeping it out of the command line) carries no IDs the hook can see and is blocked. Same shape as facet 1 and it fails closed, so it is noise rather than a hole; the workaround is to pass the message inline. Belongs to `~/Projects/dev-procedure`. Recorded here rather than fixed, because standing rule 7a as rewritten on the same day says a session touches only its own project. Kenny said "Opnemen" in the round-two AFK report. | 2026-09-04 | repaired at round two's retrospective | **closed** |
| KT5-M1 | Correction KT5, field 7, the consumer half: JobTracker builds a screen from `Form` and `FormField` with their own words, without touching this repository. **Measured by the JobTracker session, 2026-09-05, on 2.0.0: the words half passed and the form half failed.** They rebuilt their login on `Form` + `FormField` with their own labels as props, no `strings` override needed, typecheck and build clean — then their `auth.spec.js:60` failed at the second submit because the busy button never re-enables. Not the language: the latch. Became correction KT6. | 2026-09-05 | JobTracker's adoption of 2.0.0 | **closed, with a finding → KT6** |
| KT6-M1 | Correction KT6, field 7: KT5-M1 again on the version carrying the repair — JobTracker's login on `Form`, and their `auth.spec.js:60` passes. Their test on their screen; ours can see the latch open, only theirs can see that someone logs in again. | 2026-09-05 | JobTracker's adoption of the version carrying KT6 | open |
| D3 | Decided by Kenny on 2026-09-05, after 3.0.0: `STRINGS_NL` leaves `js/strings.js` and `index.js` ("Weghalen in 3.1.0" — the label said 3.1.0, but removing an export is a breaking change, so it ships as **4.0.0**; flagged in the round-three plan). S20 keeps the 72 lines available in the 2.0.0 and 3.0.0 tags; MIGRATION says so. Not built yet — the theme round (round three) was scoped first. **Closed 2026-09-07 at the AFK report, and the ID collision was worse than it looked.** Kenny answered the report's ID item with "rename the new D3", and checking which of the two the round had actually frozen showed that `docs/FEATURES.md` names D3 as `STRINGS_NL` leaving the exports — one of the eight Essential features of round five. It was never built: AR27 and the W1 brief both said D3 for the events, so W1 built those and the frozen item went unnoticed through five milestones and a report. `STRINGS_NL` is removed now, `MIGRATION.md` says where the seventy-two lines still live (S20 keeps them in the v2.0.0 and v3.0.0 tags), and a unit test pins both removals — drilled red by putting the export back. The events are **D4** from here. AR27 and `docs/REALIZATION_PLAN.md`'s W1 row both say "D3" for a different removal: `ARM_EVENT` and `DISARM_EVENT` leaving `js/components.js` and `index.js`, which is what W1 built. The two are compatible and neither was widened — but this row, `STRINGS_NL`, is still open and W1 did not touch it, so a reader who finds "D3 is done" in the round-five plan must not conclude that `STRINGS_NL` has gone. Renumbering one of them is a decision for Kenny, not for an AFK milestone. | 2026-09-05 | the first release after the theme round, or 3.1.0 if that comes sooner  **Closed 2026-09-09:** it shipped. `STRINGS_NL` is gone from `js/strings.js` and from `index.js`; the module carries a comment at `js/strings.js:312` recording the removal in 4.0.0, and `MIGRATION.md` names the replacement. | **closed** |
| KT7 | Correction KT7, approved 2026-09-05 in the round-three AFK report (nine fields "Klopt"): CI ran the hook's subset, not `npm run gates`, so `check:strings` (and `check:types:consumer`) had been red at 3.0.0 with CI green. Measure executed in `7fe7869`: CI runs the whole chain, the hook runs both missing checks, and a unit test lays every `check:*` script beside the chain and the hook. **Measured 2026-09-05 at the first CI runs after the change:** the full chain green on `bc1e369` (run 33941401074); an injected literal on branch `kt7-drill` (`js/kt7-drill.js:7`, commit `177358e`) turned the gates job red on `check:strings` (run 33940706648); the branch was deleted afterwards. | 2026-09-05 | done at the first CI run after the change | **closed** |
| KT8 | Correction KT8, approved 2026-09-05 (nine fields "Klopt", hover = one step, release held): spinner, checks and highlights fixed test-first; hover step raised; the native `<select>` list themed under `appearance: base-select`; and the foreign-colour detector Kenny asked for now runs on every fixture — it found and fixed `<hr>`, the picker's sliders and `<progress>` on its first run. **Measured 2026-09-05 at Kenny's third look:** spinner, radio and highlights gone by his eye; the select list still the platform's in his browser — "misschien door mijn browser afgedwongen … voorlopig accepteerbaar", recorded as a known limitation (Firefox and Chromium before 135 draw it themselves). Fixtures suite green in CI (run 33945120357). | 2026-09-05 | done at the third look | **closed, one known limitation** |
| R0-TYPO | **Quarantined at R0, 2026-09-06 (AFK).** TH94's typography half cannot meet its own exit criterion. Measured in `css/components.css`: `--kp-text-sm` is referenced 29 times with **two different fallbacks** — `0.875rem` at five sites (135, 223, 490, 621, 763) and `0.8125rem` at the other twenty-four; `--kp-text-xs` likewise has `0.6875rem` at line 1357 and `0.75rem` at 1435, 1678 and 1992. Declaring either name as a token forces one value on every site, so some component's text size changes whichever value is chosen — against R0's criterion that nothing shifts, and against S20. The spacing half is unaffected (`xs`, `sm`, `md` each have a single fallback) and is built. Nothing is built on top of the typography tokens until this is decided: rename the two readings apart, pick one value and accept the shift in a major, or drop the typography scale from TH94. **Confirmed blocking at R2, 2026-09-06:** the utility API ships without a font-size family for exactly this reason, and `docs/UTILITIES.md` says so in place of the missing family. | 2026-09-06 **Decided by Kenny 2026-09-07: every place keeps its own value.** The six deviating rules got their own knob at the literal they already had — `--kp-theme-status-size`, `--kp-footer-size`, `--kp-field-label-size`, `--kp-field-option-size`, `--kp-dialog-description-size` (0.875rem) and `--kp-shortcuts-label-size` (0.6875rem) — so each scale name means one thing. `kp-text-xs` (0.75rem), `kp-text-sm` (0.8125rem) and `kp-text-md` (1rem) are declared in all 24 themes; the parity gate now holds 81 names and the contrast gate carries the three as exempt lengths. The utility API gained the font-size family TH93 always listed (115 → 118 classes). TH94's other half needed no code: h1–h6 already descend strictly in all 24 themes, measured, and a test now holds it there. No text moved anywhere. | 2026-09-06 | done at the fourth mini-round after the AFK run | **closed** |
| R3-CQ | **Measured at R3, 2026-09-06 (AFK), queued rather than built around.** Converting the table's card layout from a media query to a container query needs `container-type: inline-size` on `.kp-table-wrap` and `.kp-datatable` (AR24 asked for both to be measured before and after). Measured in Chromium 141 and Firefox 145 at 1280px: **normal block flow 500px -> 500px** and **inside `.kp-datatable`, the shape this package draws, 500px -> 500px** — no change, so the conversion is built. But a wrapper a consumer puts in a shrink-to-fit box does change: as a flex-row item 279px (Chromium) / 328px (Firefox) -> **0**, as an inline-block 207/244px -> **0**, in an auto grid track 389/414px -> 250px. No consumer markup has to change and nothing this package renders moves, so this is not a quarantine; it is a silent regression waiting for the one consumer who does it. `docs/USER_GUIDE.md` says so under "What the container query costs", and `tests/tables.spec.mjs` holds the two shapes that must not move. Open question for Kenny: leave it documented, or have `.kp-table-wrap` also declare a width so a shrink-to-fit parent cannot collapse it (which would change how the wrapper sizes for everyone instead of for the few). | 2026-09-06 **Decided by Kenny 2026-09-07: add a floor knob.** `--kp-table-wrap-min` on both container carriers, defaulting to `auto` — the initial value in every context, so the default is a true no-op and the drill confirms the wrapper still collapses to 0 without it. Measured before deciding, with four candidate repairs side by side: none restores the natural width, because the contents are precisely what containment stops counting — `100%` gives the whole parent (800px in a flex row) and 0 as an inline-block, `min-content` gives 0, and a fixed 20rem gives 320px. So the knob is documented as a floor rather than a repair, in `docs/USER_GUIDE.md` and in the rule's own comment. Test-first: red at 0 where it expected 320. | 2026-09-06 | done at the fifth mini-round after the AFK run | **closed** |
| R0-PRINT | **Found at R3 and again at R4, 2026-09-06 (AFK); repaired the same day at the merge.** `tests/promises.spec.mjs`'s print case is red in both browsers on `main` as of `15f7307`: on `/showcase/themes/cyberpunk.html` under print media `--background` reads `hsl(258, 40%, 6%)` where the test asks for white. Cause measured, not guessed: R0 moved `css/_rules.css` inside `@layer kp.base`, so the `@media print { :root { … } }` block at `css/themes.css:3049` is layered while the theme token blocks above it are not — an unlayered declaration beats any layer (AR17 took that consequence deliberately for consumers, and this is the package doing it to itself). Proved by injecting the same rule unlayered into the live page: `--background` then reads `hsl(0, 0%, 100%)`. The repair is one move — the print block, and any other rule that has to beat a token declaration, out of the layer — and it is R0's to make. Nothing in R3 touches it. | 2026-09-06 **Repair:** the block moved to `css/_print.css`, concatenated after the authored rules and outside every layer, so it wins by source order as TH36 always meant. `tests/promises.spec.mjs` green in both browsers afterwards. | 2026-09-06 | done at the round-four merge | **closed** |
| KT9 | Correction KT9, approved 2026-09-06, all nine fields "Klopt": Claude hand-built the v3.1.1 GitHub release without first reading `.github/workflows/release.yml`, publishing an incomplete `SHA256SUMS` (3 of the canonical 10 files) for about ten minutes before catching and rebuilding it from `npm run checksums`, verified against the tagged tree. Measure: read a trigger's own workflow definition before replicating its job by hand. | 2026-09-06 **Measured 2026-09-07 at the v3.2.0 tag push, the moment field 7 named: the measure held.** Claude read `.github/workflows/release.yml` before touching anything, wired the dist bundle into its asset list per M1's amendment instead of attaching files by hand, and let the workflow build the release. The draft carries six assets and a `SHA256SUMS` of **34 lines**; every one of the 34 was verified against the tagged tree (0 mismatches), as were all five attached files. Nothing was hand-picked. | 2026-09-06 | done at the v3.2.0 tag push | **closed** |
| MR-R6-1 | **Quarantined at R6, 2026-09-06 (AFK).** T10 froze the story renderer at "exactly seven constructs" on a measurement that missed an eighth: underscore emphasis. Measured across the 24 documents while building the renderer — **9 spans in 6 stories**: `themes/dark/anatomy.md:32` (`_lighter_`) and `:62` (`_lighter_`), `themes/pastel/anatomy.md:10` (`_ground_`) and `:48` (`_cleaner_`), `themes/cyberpunk/anatomy.md:22` (`_occasional_`), `themes/forest/anatomy.md:12` (`_map_`), `themes/woodblock/anatomy.md:9` (`_bero-ai_`), `themes/retro/anatomy.md:14` (`_is_`). Refusing it means 6 of the 24 stories cannot be rendered at all; rendering it silently would be the eighth construct arriving without a decision. So `gates/site/markdown.mjs` refuses it by default, naming this entry, and renders it as `<em>` only when the caller passes `{ emphasis: true }` — the site generator's use of that flag is the thing to approve or reject. The alternative is to rewrite nine spans as bold and keep T10 exactly as frozen. | 2026-09-06 **Decided by Kenny 2026-09-07: correct T10 to eight constructs.** The measurement was wrong in three of eight figures (bold 327 not 323, code 193 not 189, emphasis 9 not counted); correcting a miscount is not widening a frozen choice. The renderer renders `<em>` with no flag, the site passes none, and T10 carries the dated amendment in the same commit as the code (FORM_PROTOCOL §5.4). | 2026-09-06 | done at the first mini-round after the AFK run | **closed** |
| R4-LOCALE | Observed while building TH103's manifest gate, 2026-09-06. The gate's expected list is every concrete export target under `css/` or `js/` — 28 files. `js/locale.js` is not among them because the package does not export it, and yet `js/datepicker.js` imports it, so a consumer vendoring the date picker by hand copies a file that breaks on the import. Either it becomes an export (and the gate then demands it in the manifest by itself) or the manifest's rule has to follow imports as well as exports. Left as it is, because both answers widen what the package promises. | 2026-09-06 **Decided by Kenny 2026-09-07: the gate follows imports.** `gates/check-manifest.mjs` now walks the import graph from every copyable export and expects what it reaches, so being an export is no longer the property that matters — having to be copied is. Chosen over exporting the file because a measure that fits only the place a fault showed up meets you again elsewhere, and this one adds no public name. Measured before deciding: from the 33 copyable exports the walk reaches exactly one file that is not itself one, `js/locale.js`; the same walk through the React files finds `fx/when.js`, the same shape, which stays out because nobody vendors `.jsx`. The gate reports `33 copyable exports plus 1 file(s) they import`, and both directions are drilled — the file taken back out of FILES, and an export made to import a module nothing else reaches. | 2026-09-06 | done at the sixth mini-round after the AFK run | **closed** |
| R5-BADGE | **Quarantined at R5, 2026-09-06 (AFK).** A coloured status badge and TH109's zero-inline-style bar cannot both hold on a hand-written page. Measured: `css/components.css:364` declares `.kp-badge` with the muted plate and there is no `[data-status]` rule anywhere in that stylesheet (0 matches); the React badge paints the plate as an inline style (`components/badge.jsx:43`), and the framework-free channel does the same by hand (`showcase/specimens.mjs:77`). So a server-rendered page that wants the seven status colours has to write `style="background: var(--status-…)"`, which is exactly what TH109 forbids. R5's ten example pages therefore use uncoloured `.kp-badge` and say what they mean in words, which DI4 asks for anyway. The three options: (1) generate a `.kp-badge[data-status='<name>']` rule per status into `css/components.css`, so both channels take the plate from a class and no inline style is needed; (2) widen TH109's exception list to allow `background`/`color` on a badge, which reopens the door the round closed; (3) leave it — status colour is available in React and to anyone who accepts one inline style. Not decided here: it changes a component stylesheet, which is not R5's milestone. | 2026-09-06 **Decided by Kenny 2026-09-07: generate the seven rules, and React follows.** `css/components.css` gained a `.kp-badge[data-status='<name>']` rule per name in `STATUS_NAMES` — the one list `gates/check-contrast.mjs` already holds the 24 themes to — and a unit test lays the rules beside that list in both directions. `components/badge.jsx` writes the inline plate only when a consumer points `tokenPrefix` at their own token family, because those names have no rule and dropping it would take their colours away silently. Verified in the browser before the form: all seven statuses paint identically via the class and via the inline style. Test-first (standing rule 8): the framework-free channel read the muted plate before the rules existed. Four drills, all red — a rule removed (browser and unit), a rule for a name that is not a status, and a rule painting only half its own token pair. The showcase specimens dropped three now-redundant inline styles. | 2026-09-06 | done at the third mini-round after the AFK run | **closed** |
| MR-R6-2 | **Found at R6, 2026-09-06 (AFK), while measuring the site's own pages against DI11; recorded rather than repaired.** Every component page carries the machine-sourced tables, and at 360px they made the whole document scroll sideways — the props table on `spinner` measured 525px wide. Repaired on the site's side: `gates/generate-site.mjs` puts every generated table in the package's own `.kp-table-wrap`, and 41 of the 42 pages then stay inside 360px. The one left over is not the site's: `.kp-shortcuts` (`css/components.css:1498`) is `width: min(28rem, 92vw)` with `padding: 1.25rem` and no `box-sizing`, so at a 360px viewport the sheet is **373px wide** and the page scrolls by 13px — SC 1.4.10, on a component rather than on a page. The one-line repair (`box-sizing: border-box`) also narrows the sheet's content by 40px at every viewport above 500px, which is a visible change to a shipped component, so it is a decision and not a silent fix. `.kp-palette` is unaffected: it has no padding of its own. | 2026-09-06 **Decided by Kenny 2026-09-07: border-box, with the default width raised to match.** `box-sizing: border-box` alone would have narrowed the sheet by 42px on every viewport above ~487px, which is visible behaviour of a shipped component; raising the default from `28rem` to `30.625rem` — exactly the 40px of padding and 2px of border that used to sit outside it — leaves the wide sheet at the same 490px and changes only where it was broken. Measured in the browser before and after: 360px viewport 373 → 331, 768px and 1280px 490 → 490. Test-first per standing rule 8 (red at 364 against a 360px viewport), and both halves of the fix drilled red on removal. `--kp-shortcuts-width` now means the total width; MIGRATION.md says so. | 2026-09-06 | done at the second mini-round after the AFK run | **closed** |
| MR-W4-1 | **Found at W4 of round five, 2026-09-07 (AFK), while reading this package's own pages rather than its gates; recorded rather than repaired, because AR32 is frozen.** AR32 gives `.kp-button`, `.kp-badge`, `.kp-tag`, `.kp-health` and `.kp-copyable` one shared `max-inline-size: 100%` plus `overflow-wrap: anywhere` (`css/components.css:270-276`). `overflow-wrap: anywhere` is the half that was chosen deliberately — AR32's own note says `anywhere` rather than `break-word` because only `anywhere` takes the min-content contribution down, which is what a flex item's automatic minimum size is measured against. That is exactly why it also lets a one-word label break in the middle. Measured on `examples/list-with-form.html` at a 1100px viewport, in Chromium: the status badge reading **Overdue** is 40px tall against an 18px line — it renders as `Overd` / `ue` inside its pill. With `overflow-wrap: anywhere` taken out of that rule and nothing else changed, the same badge is one line and the table reads correctly; the rest of the page is unchanged. `.kp-badge` carried no `overflow-wrap` at `v3.2.0` (`git show v3.2.0:css/components.css`, the `.kp-badge` block), so this arrived with 4.0.0. A sweep of all 42 site pages, the 10 example pages and the showcase for a single-word `.kp-badge`, `.kp-tag`, `.kp-health`, `.kp-button` or `.kp-menu__item` taller than 1.5 lines found this one instance and no other. No gate can see it: nothing measures a label's line count, and `tests/overflow-floor.spec.mjs` measures the opposite direction — that the element does not push the page out. **Options.** (a) Leave it: the floor is worth a broken word, and consumers give a status column room. (b) `white-space: nowrap` on `.kp-badge` alone, keeping `anywhere` for the other four — a badge is a pill and a pill does not wrap; it costs a badge whose text is genuinely too long for its column, which then overflows instead of wrapping. (c) `overflow-wrap: break-word` for `.kp-badge` alone — it breaks only when the word cannot fit at all, but it does not lower the min-content contribution, so a badge inside a flex row can push that row out again, which is the fault AR32 exists about. (d) Widen the example's status column and treat the badge as correct. **Consequences for built work.** W0 built and drilled the shared rule; all four options keep that rule and its test (`tests/overflow-floor.spec.mjs`, 10 assertions) intact — (b) and (c) add a second declaration for one selector, which is the 'four copies' shape AR32 refused, so it is a decision and not a tidy-up. Nothing in W4's own suite depends on the answer. | 2026-09-07 | the first mini-round after the AFK run  **Answered by Kenny on 2026-09-09 (REL1): laten staan — leave it.** Claude re-measured it in the browser first, and the case had moved: the three badges on `examples/list-with-form.html` are one line (22 px) at 1400, 1100, 700, 500 and 360 px, and a sweep of all eleven example pages found no badge, tag or health indicator over one and a half lines. The mechanism is still live below roughly 68 px of container width. Both proposed repairs were measured in the shape AR32 exists about — four badge cells sharing a 200 px row: with `overflow-wrap: anywhere` the row fits exactly (scrollWidth 200 against clientWidth 200) and the badge is 40 px tall; with `white-space: nowrap` and with `overflow-wrap: break-word` alike the badge is 22 px and the row wants 283 px in a 200 px box. So the two fixes each restore the overflow AR32 removed, on every page, to repair a break that no shipped page currently shows. AR32 stands unchanged, and the rule keeps its shared form. | **closed** |
| MR-NOTCH | **Opened and closed 2026-09-07.** Kenny asked where the cyberpunk button's bevel had gone, having approved AR30 without ever being shown the shape. Two causes: the site publishes from `main`, which is still 3.2.0, and AR30 had left the bevel off because `clip-path` clips the focus outline (784 painted pixels to 0). Measured rather than assumed this time: a ring drawn INSIDE the box survives the clip, and over 24 themes x 4 button fills every pair keeps at least one half at 3.0 or better against the fill (5.00-18.30, always `--focus-ring-contrast`). **Kenny chose the bevel with the inset ring, and chose to widen the round to the second register.** Widening it paid: the retro register erased the same ring half one layer up, its focused and unfocused `box-shadow` were identical, and neither ring fixture loaded `css/retro-register.css` at all -- rule 7e for the third time this round. Both the ring check and the painted-pixel count were also passing for the wrong reason and now measure the difference focus makes. Recorded as a dated amendment in `docs/ARCHITECTURE_DECISIONS.md`. | 2026-09-07 | closed in the same commit as the fix | **closed** |
| KT10 | **Open until the measurement happens.** Correction KT10, approved by Kenny on 2026-09-07, all nine fields unchanged: a frozen Essential feature (D3, `STRINGS_NL` leaving the exports) went unbuilt because one ID meant two things, and measuring where else that sat found 18 genuine collisions among 270 IDs across six documents — T1..T16, D3 and F1. The measure is `gates/check-ids.mjs`, blocking, plus giving `docs/INVENTORY.md` its own `INV-` namespace. The gate proved itself on real data before any cleanup: it went red on all 18, and red again on an injected `TH110` naming both documents. **Field 7's measurement is still owed:** at the next Phase 2 that freezes a feature list, the gate gets a deliberately colliding new ID and must go red naming both documents. | 2026-09-07 | the next Phase 2 freeze | **closed 2026-09-07** — measured at round six's Phase 2 freeze: a heading `## TH129 · …` injected into `docs/SCOPE.md` made `gates/check-ids.mjs` exit 1 with `TH129 is defined in docs/FEATURES.md and docs/SCOPE.md`; restored, exit 0. Two findings beside the pass: a bold-paragraph shape (`**TH129 · …**`) is not a definition to the gate — only a table row or a heading is, by design — so the first attempt at the drill measured nothing and had to be reshaped; and the drill caught a real collision on the way, `TH132` defined by its FEATURES.md row and by the heading of its own mini-round entry, excused with a reason like TH47 before it. |
| KT15-M1 | Correction KT15, field 7: at the next lift built from a demo after the deviation form (blueprint v3, once approved), the ratification carries a demo-against-register audit that finds zero deviations not first asked. Until Kenny answers the deviation form of 2026-09-08 the four built registers (phantom, retro, terminal, brutalism) keep their deviations and `main` stays at retro (`8695642`). | 2026-09-08 | the next lift's ratification  **Closed 2026-09-08 at the combined ratification:** `docs/audits/DEMO_FIDELITY_ROUND_SIX_2026-09-08.md` gathered every lift's findings in one document before anything was ratified, and Kenny answered them there (R1-R5 agreed, R6 and R7 shown to him, then the band and the menu caption built from his answers). No deviation was made without first being put to him, which is what the measure asked for. `main` no longer stands at retro. | **closed** |
| R6-Q8 | **A7, open.** Phantom's demo skews the button element itself (border and hit area) and counter-skews the label; the package skews only the plate on `::before`. Kenny answered the deviation form with "show me the difference on a web page", so the comparison page is built and the choice returns in the next form. Phantom's button stays as built until he answers. | 2026-09-08 | the next form | **closed 2026-09-08** — Kenny chose the demo on the comparison page: the element carries the skew, the label is set upright on its own span, and the phantom spec now reads the label (S49-5). |
| TH111-M1 | **The button sizes, reopened by A4 (Demo exact).** The frozen bar reads "the three sizes differ in height in all 24 themes"; terminal's and brutalism's demos give every button one minimum height, which flattens that difference. The amendment: the normal size is the demo's height, and small and large differ from it in padding and font size, which `tests/button.spec.mjs` now measures instead of height alone. Dated amendment shipped in the same commit as the change (FORM_PROTOCOL §5.4). | 2026-09-08 | at the commit that lands A4  **Closed 2026-09-09:** the dated amendment shipped — `docs/FEATURES.md` line 286 carries it, and `tests/button.spec.mjs` lines 165-173 now accept either a height difference or a difference in font size and padding, measuring both rather than height alone. | **closed** |

## TH132 · The infinite pulse gets a ceiling — Later (2026-09-07)

Rated Later by Kenny at round six's Phase 2, round two. `.fx-pulse` in
`css/cyberpunk-register.css` runs `infinite`; the proposal is a knob
`--fx-pulse-cycles` with default 3 and the pulse in TH129's DI5 report.
Untouched until Kenny reopens it; TH129 reports its flash rate regardless.

## MR-R6-COMPARE · The concept demo side by side, old against new (2026-09-07)

Kenny, during C0: "Kunnen we de demopagina's net zoals de showcase pagina
in de helft in twee kappen? Dan kunnen we de oude vs de nieuwe versie
links vs rechts vergelijken." A request made in AFK mode, so it is
recorded here and built at C5 rather than argued: the assembly milestone
is the page Kenny opens to judge, and this is how he wants to judge it.

**What it is.** A compare page beside the concept demo: two frames on
one page, the left rendering the concept page under the 4.0.0
stylesheets of the same theme, the right under the current ones,
`?theme=<name>` selecting both. The 4.0.0 stylesheets are the release's
own files, vendored under `showcase/baseline/4.0.0/` and verified by a
gate against the committed copy of that release's `SHA256SUMS` — the
package comparing itself to its own promise (S20). Same name on both
sides, because the new cyberpunk replaces the old under its name (S39).

**Kenny's second reading (2026-09-07, after the first build):** not two whole pages — "de compare.html pagina gaat nu enkel de verschillen tussen de oude en de nieuwe versie highlighten … toon enkel de verschillen waar toepasbaar. En zeg het er duidelijk bij wat er anders is. Nog altijd side by side", full width, scrolling together. The page is now a measured diff per theme (`gates/generate-compare.mjs`) with a pair of specimen frames showing only the touched sections.

**Where it lands.** TH126 grows by the compare page; C5's exit criterion
gains "the compare page shows 4.0.0 on the left and the current build on
the right for the theme in the query, and Kenny has opened it". The
frozen list is amended by this note rather than reopened, because the
request extends a feature it does not change.

## R6-Q1, R6-Q6 and R6-Q8, answered 2026-09-08

Kenny's answers at the release form, before the version was set:

- **The texture ceiling stays 0.06.** Three themes carry their own — phantom
  0.14, blueprint 0.18, dark 0.35 — each named in `gates/config.json` with
  the approved demo that measured it. The alternative, raising DI9 until
  nobody meets it, would have turned a decision into an absence of one.
- **Woodblock's font budget is raised for that theme alone**, to 2.5 MB
  against a measured 2,375,188 bytes, so its two Japanese families ship a
  real bold face each. A synthesised bold thickens a Japanese stroke where
  a real face redraws it. `gates/check-fonts.mjs` reads a per-theme budget
  the way the texture gate reads a per-theme ceiling.
- **The concept pages' footer counts twenty-five registers.** Every page
  counted the registers as they stood on the day its own demo was
  approved — three, six, seven — and one still said twenty-four themes.
  The line states a fact about the package, not a design choice, so it is
  corrected rather than frozen.
- **The version is 5.0.0, after the three procedure steps** that have not
  run for this round: the test-gap audit, the documentation pass and the
  field test. Not another alpha.
