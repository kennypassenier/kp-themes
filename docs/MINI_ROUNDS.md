# Mini-round and measurement queue — kp-themes

Open items that must not live only in a conversation: pending
measurements from correction forms, and mini-rounds that reopen a frozen
decision. An entry leaves this list only when it has actually happened,
with the result recorded.

| ID  | What | Opened | Triggers at | Status |
| --- | ---- | ------ | ----------- | ------ |
| KT1-M1 | Count, in a decision form, how many claims assert a fact about code and how many carry a file:line or an explicit second-hand label. **Taken at the Phase 7 gate form, 2026-09-04: nine claims, nine sourced.** They matched. Kenny's answer at H3 was that one matching count does not undo a measure already broken once, so KT1's fallback applies from here regardless. | 2026-09-03 | done at the Phase 7 gate | **closed** |
| KT1-M2 | Whether KT1 field 8's fallback triggers, after the measure failed once following its own approval. **Decided 2026-09-04 at the Phase 7 gate: it triggers.** From now on every claim in a form for this project carries its source, or becomes its own item in that form. Recorded in `docs/CORRECTIONS.md` and in the project rule in `CLAUDE.md`. | 2026-09-03 | done at the Phase 7 gate | **closed** |
| F1 | **Nothing in this package styled a link.** Closed by L6 on 2026-09-04: `css/_rules.css` now styles `a`, `a:visited` and `::selection`, `--link` and `--link-visited` are derived per theme, four contrast pairs gate them, and a browser test measures the painted link colour against the painted background on all seven fixtures. The gate no longer verifies a promise the package does not keep. | 2026-09-03 | closed at L6 | **closed** |
| MR-NPM | Mini-round of 2026-09-04: Kenny dropped JobTracker's build step as a supported environment and decided this package publishes nothing to npm at all. Outcome, all approved: S19 added (the package does not prescribe how it is consumed); two constraints struck from S8 (`allow-git`, the Tailwind `@source`); S10 rewritten to promise only what this package can prove; M1's Dependabot measurement closed unmeasured because the mechanism it watched no longer exists. Recorded in `docs/SCOPE.md` and `docs/FEATURES.md` with dated amendment notes. | 2026-09-04 | — | closed |
| MR5-PAGES | The showcase is published on GitHub Pages, and Claude enabled that setting. Kenny approved it in the AFK report of 2026-09-04. The site is assembled by `.github/workflows/pages.yml` from `showcase/` plus the three directories it loads, so `docs/` is not published. Live at https://kennypassenier.github.io/kp-themes/ | 2026-09-04 | done | **closed** |
| KT2 | Correction KT2: a gate checked one half of a two-halved property. Measurement of field 7 done in the same session — 9 violations before, 0 after, all 28 interactive states at 10.1 to 12.1, and the two tests that pinned the fault rewritten into their opposite. | 2026-09-04 | done at the repairing commit | **closed** |
| AR8-D1 | Defect found by the Phase 4 critic pass and verified by hand on 2026-09-04: `scripts/check-contrast.mjs` discovers themes with a pattern matching lowercase letters only, so a theme named `high-contrast` or `topo2` is silently skipped while the run reports every theme passing; the floor test only fires below five themes, so with seven present an eighth goes unnoticed. **Closed 2026-09-04 in L1.** Three tests were written first and failed against the broken version; the pattern now accepts digits and hyphens, and the theme count is checked against `themes/order.json` instead of a floor of five. The gate moved to `gates/check-contrast.mjs` per S18. | 2026-09-04 | done in L1 | closed |
| PROC-H1 | Defect in the shared procedure repository, found on first use 2026-09-04: `hooks/check-commit.sh` decides a command is a commit by testing whether the command string contains the words, so any command whose text merely quotes an example message is blocked. Writing `docs/REALIZATION_PLAN.md` triggered it, because that document quotes one. It fails closed, so it is noise rather than a hole, and the git-native layer is unaffected because it inspects the real message. Belongs to `~/Projects/dev-procedure` and affects every project that installed the hook; raise it there rather than patching a local copy. **Two more facets, both hit on 2026-09-04.** (1) The hook gates the tree as it stands *before* the command runs, so a compound command that formats and then commits is always judged on the unformatted tree and can never succeed. (2) Worse: it blocks the whole command, so the setup steps in it — creating a branch, for instance — never run either, and the retry then lands the work on whatever branch was already checked out. That happened here: an L2 commit intended for a feature branch landed on `main` and had to be moved. The workaround is discipline, not code: never put setup and a commit in one command. | 2026-09-04 | the next session opened in dev-procedure, or its retrospective | open |
| DI4-SCOPE | How strict the colour-deficiency check on the status badges must be. Decided 2026-09-04: the gate keeps measuring the one pair where confusion is harmful (offer against rejected), which all seven themes clear. Widening to all 21 pairs would delete six pale palettes rather than tighten a check — 13 to 16 pairs fall under the floor in five themes, 2 in cyberpunk, because cyberpunk is saturated and the others are pale by design. Recorded with the full measurements in `docs/DESIGN_INVARIANTS.md` under DI4. | 2026-09-04 | done | **closed** |
| L3-EXIT | L3's exit criterion said the compliance table reads pass everywhere, which L3 could not reach alone. Decided 2026-09-04: clean up and enforce. DI9 was the last ungated row; measuring it found 42 colour literals duplicating tokens, three of them already drifted. All 42 now read from tokens, `gates/check-layers.mjs` keeps it that way and has been red once, and the compliance table has no ungated rows left. Nothing on screen changed — the largest shift measures 0.37 after compositing. | 2026-09-04 | done | **closed** |
| KT3-M1 | Correction KT3, field 7, part (c). **Measured 2026-09-04, and the honest result is that the discipline was not applied while round two was being built:** roughly sixty browser tests were written without a recorded drill. Performed afterwards on the two most at risk — the DataTable's card layout (delete the `display: flex` rule, both channels go red) and the combobox's virtual focus (delete the `aria-activedescendant` line from js/listbox.js, the framework-free case goes red while React correctly stays green, because the two channels are two implementations). Both drills are now recorded in the tests themselves. What this measurement actually showed is that the rule gets skipped under AFK volume, which is what field 8 has to answer. Part (b) was measured at the moment of building: the pre-fix `body { font-family }` put back into `showcase.css` makes `gates/check-layers.mjs` exit 1 naming line 9, and 0 once restored. | 2026-09-04 | done during round two, late | **closed, with a finding** |
| KT4-M1 | Correction KT4, field 7: JobTracker's own typecheck against the shipped declarations. **Measured by the JobTracker session, 2026-09-05, on 2.0.0: passed, and stronger than asked** — `tsc -p jsconfig.json` clean, then again with `--skipLibCheck false` because their config skips library checks by default: clean, zero errors, none in `@kp-soft/themes`. Their config: strict, NodeNext, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `checkJs`. The declarations hold up on their own rather than being skipped. | 2026-09-04 | JobTracker's adoption of 1.1.0 | **closed** |
| TH47 | The visual filter builder was the one candidate Kenny could not rate from the description: "kan je hier meer info over geven, met voorbeelden en misschien een link naar een live voorbeeld ergens? want ik snap niet wat je bedoelt" (2026-09-04). Answered with the Notion and Airtable equivalents and a live demo; awaiting his rating. | 2026-09-04 | the round-two rating form | open |
| PROC-H2 | Fourth facet of PROC-H1, found 2026-09-04 while building round two: the traceability half of `hooks/check-commit.sh` looks for bracketed feature IDs in the COMMAND text, so a message passed with `-F file` (or any other way of keeping it out of the command line) carries no IDs the hook can see and is blocked. Same shape as facet 1 and it fails closed, so it is noise rather than a hole; the workaround is to pass the message inline. Belongs to `~/Projects/dev-procedure`. Recorded here rather than fixed, because standing rule 7a as rewritten on the same day says a session touches only its own project. Kenny said "Opnemen" in the round-two AFK report. | 2026-09-04 | repaired at round two's retrospective | **closed** |
| KT5-M1 | Correction KT5, field 7, the consumer half: JobTracker builds a screen from `Form` and `FormField` with their own words, without touching this repository. **Measured by the JobTracker session, 2026-09-05, on 2.0.0: the words half passed and the form half failed.** They rebuilt their login on `Form` + `FormField` with their own labels as props, no `strings` override needed, typecheck and build clean — then their `auth.spec.js:60` failed at the second submit because the busy button never re-enables. Not the language: the latch. Became correction KT6. | 2026-09-05 | JobTracker's adoption of 2.0.0 | **closed, with a finding → KT6** |
| KT6-M1 | Correction KT6, field 7: KT5-M1 again on the version carrying the repair — JobTracker's login on `Form`, and their `auth.spec.js:60` passes. Their test on their screen; ours can see the latch open, only theirs can see that someone logs in again. | 2026-09-05 | JobTracker's adoption of the version carrying KT6 | open |
| D3 | Decided by Kenny on 2026-09-05, after 3.0.0: `STRINGS_NL` leaves `js/strings.js` and `index.js` ("Weghalen in 3.1.0" — the label said 3.1.0, but removing an export is a breaking change, so it ships as **4.0.0**; flagged in the round-three plan). S20 keeps the 72 lines available in the 2.0.0 and 3.0.0 tags; MIGRATION says so. Not built yet — the theme round (round three) was scoped first. | 2026-09-05 | the first release after the theme round, or 3.1.0 if that comes sooner | open |
| KT7 | Correction KT7, approved 2026-09-05 in the round-three AFK report (nine fields "Klopt"): CI ran the hook's subset, not `npm run gates`, so `check:strings` (and `check:types:consumer`) had been red at 3.0.0 with CI green. Measure executed in `7fe7869`: CI runs the whole chain, the hook runs both missing checks, and a unit test lays every `check:*` script beside the chain and the hook. **Measured 2026-09-05 at the first CI runs after the change:** the full chain green on `bc1e369` (run 33941401074); an injected literal on branch `kt7-drill` (`js/kt7-drill.js:7`, commit `177358e`) turned the gates job red on `check:strings` (run 33940706648); the branch was deleted afterwards. | 2026-09-05 | done at the first CI run after the change | **closed** |
