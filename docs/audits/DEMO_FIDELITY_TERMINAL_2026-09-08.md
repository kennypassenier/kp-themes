# Terminal ("Green Phosphor") — demo vs. package audit

Source of truth for "the demo": `/tmp/claude-1000/-home-kenny-Projects-kp-themes/0f370a8a-aa0a-4f17-8db1-4774735a6a56/scratchpad/terminal-demo.html`
(`<title>Green Phosphor</title>`, line 2 — the only terminal-named `.html` in the scratchpad, and its title matches). All demo line numbers below refer to this file.

Package files read in full: `css/terminal-register.css` (1263 lines), `themes/terminal/tokens.json` (414 lines), `themes/terminal/anatomy.md`, `tests/register-terminal.spec.mjs`, `themes/hooks.json` (terminal entry), and the relevant parts of `js/effects.js`, `js/strings.js`, `examples/concept.html`, `showcase/concept-demo.json`, `css/components.css`. A prior draft of the register, `terminal-register.bak` (found in the scratchpad, dated 06:58 vs. the shipped file's 1263-line version), was diffed against the shipped file to pin down two of the already-known "forced by fixture tests" deviations exactly.

No repo files were modified. Colour arithmetic (hex↔HSL, WCAG contrast) was done with small throwaway Node scripts, not committed anywhere.

---

## Class A — the demo shows it, the package changed or dropped it

| # | Element / token | Demo (line, value) | Package (file:line, value) | Recorded reason | What goes red if restored |
|---|---|---|---|---|---|
| A1 | Boot **mechanism** | L727–733, L749–756: five fixed POST lines (`KP-THEMES BIOS v5.0.0`, `MEMORY TEST ... 640K OK`, `PHOSPHOR PROFILE ... terminal`, `CRT WARM-UP ... OK`, `READY.`) revealed cumulatively, one every 190 ms | `js/effects.js:789–796`: a single dictionary line plus a **randomised percentage counter** (`pct += 7 + random(9)` every 110 ms) ending in the word "OK" | Anatomy.md only discloses the *text* swap ("the module's line is the dictionary's"); the swap from a fixed 5-line POST to a percentage-counting mechanism is not written down anywhere | `tests/register-terminal.spec.mjs:94` — `await expect(boot.locator('.kp-boot__line')).toContainText(/%/)` — fails outright if the demo's non-percentage lines are restored |
| A2 | Boot **text** | L728–732 (see A1) | `js/strings.js:254,256`: `arrivalLine: '▶ Calibrating neural uplink'`, `arrivalReady: 'OK'` — one generic line shared by every "boot"-arrival theme (synthwave, terminal) | Recorded in anatomy.md line 60–64 (KT5: a theme's own boot text is copy, not data) | Same as A1 |
| A3 | Man page **synopsis and closing note** | L590 (`terminal — a phosphor CRT theme for kp-themes … SYNOPSIS · terminal [--density compact] [--register plain]`) and L602 (`A third hue is out of bounds. …`) | Not present at all. `examples/concept.html:253–296` (`.kp-spec`) carries only a `<dl>` of token swatches — no synopsis paragraph, no closing note | Anatomy.md line 84 only names the `NAME` heading as dropped copy (KT5); the synopsis and note paragraphs are a second, un-itemised instance of the same rule | None known — `showcase/concept-demo.json` only requires `class="kp-spec"` and a swatch to be present, not the prose |
| A4 | The demo's own prose throughout | Headline L577 "Everything is a character cell.", lede L578, side note L568 "REC · VT220 · 80×24 · 9600 8N1", statusline L572–574, brand tag L539 "TTY//06", dossier prose L659–660, checkbox label L645, footer fine print L687 | None of this text is reproduced anywhere the package ships or tests. `examples/concept.html` (the fixture every register test opens) carries the **Signal/cyberpunk demo's own copy** ("Stop looking like a theme.", "Chromium/Firefox…", "dossier.kp-themes-06", etc. — `examples/concept.html:233,246–251,346`) regardless of which theme is selected via `?theme=terminal` | Not recorded for terminal specifically. Project-wide: `showcase/concept-demo.json` (KT11) is explicitly a **structural** inventory ("every element … with the marker its rendered HTML shows"), not a copy inventory | None known — `tests/register-terminal.spec.mjs`'s "approved inventory" test (line 285) and the KT11 gate only check for marker strings (`class="kp-nav__brand"`, `data-kp-reveal="headline"`, …), never literal text |
| A5 | `<select>` **arrow** | L410: two literal `linear-gradient` triangles in `--phos-bright`, positioned at `calc(100% - 17px)`/`calc(100% - 12px)`, with `appearance: none` on the element | `css/terminal-register.css:589–591`: only `select.kp-field__input::picker-icon { color: var(--primary); }` — no `appearance: none`, no drawn chevron. Depends on `css/components.css:811–817`'s `@supports (appearance: base-select)` gate (Chromium 135+ only); everywhere else the browser's own native arrow shows, uncoloured | Package-wide policy from **KT8** (`docs/CORRECTIONS.md:894–899`) — not terminal-specific, and not written into terminal's own anatomy.md | None known with certainty. KT8 mentions a fixture-wide "foreign colour" detector (`docs/CORRECTIONS.md` ~L905) that might catch a reintroduced hard-coded chevron colour, but its exact test name wasn't confirmed |
| A6 | `--panel-2` (input wells, dropdowns) | L18: `#131a13` → hsl(120, 15.6%, 8.8%) | `--secondary` — `themes/terminal/tokens.json:42–43`: `hsl(120, 8%, 11%)` = `#1a1e1a`. Used for the same UI role: `css/terminal-register.css:503` (field input), `:341` (nav dropdown), `:677` (dialog/popover/menu/toast/tooltip/confirm) | No reason recorded | None known — no test pins the exact hex of `--secondary` |
| A7 | `--phos-hot` (the one "brightens under pressure" colour, DI3 opt-out) | L21: `#9bff9b` = hsl(120, 100%, 80.4%), reused unchanged for button hover, plate hover *and* the danger hover text (`color:#ff8080`, L232, hsl(0,100%,75.1%)) | Three separately derived tokens instead of one flat colour — `css/terminal-register.css:60–62`: `--kp-hot: hsl(from var(--foreground) h s 80%)` → `#a1f7a1`; `--kp-hot-plate: hsl(from var(--primary) h s 80%)` → `#9efa9e`; `--kp-hot-alarm: hsl(from var(--destructive) h s 75%)` → `#f98686`. All three are measurably *less saturated* than the demo's flat hots (85–90% vs. the demo's 100%) | DI9 discipline is recorded in the register's own header comment (relative-colour derivation, no literal hex) and in anatomy.md's DI3 section — but the resulting three-way colour drift from the demo's single value was never itemised | None known — no test pins the exact hex of the hover/pressed "hot" state |
| A8 | Button `min-height: 2.9rem` and `box-shadow: none` on `.kp-button` (base rule, all variants) | L212, L219 (implicit via `.btn` base rule applying to every button on the page) | Removed from the base rule. Confirmed by diffing the prior draft `terminal-register.bak:376–388` against the shipped `css/terminal-register.css:376–406`: `min-height: 2.9rem` and `box-shadow: none` are gone from `.kp-button`; `min-height: 2.9rem` survives only scoped to `[data-kp-surface='hero'] .kp-row > .kp-button` (`:402–405`). As a further consequence (not previously called out), vertical padding also reverts to the base component's `0.375rem` (`css/components.css:447`) instead of the demo's `0.55rem`, since the register now only overrides `padding-inline: 1.4rem` | Kenny's brief for this task already names this deviation as fixture-driven | `tests/button.spec.mjs:156` — "the three sizes differ in height in all 24 themes [TH111]" — and the two-channel focus ring composed in `css/components.css:477–481` (a flat `box-shadow: none` on `.kp-button` would blank the ring's inner channel) |
| A9 | Nav link `box-shadow: none` on hover/focus | Not applicable in the demo (the demo's `.menu-link` never sets `box-shadow` at all — inverse video only) | Per Kenny's brief, an earlier draft of the register set `box-shadow: none` on `.kp-nav__link:hover`/`:focus-visible`, later removed. **Could not independently verify**: `terminal-register.bak` — the only prior snapshot available — shows no such rule in that state (only `.kp-nav__menu`'s unrelated `box-shadow: none`, line 344, which is unchanged in the current file and is correct — a flat dropdown panel). Current `css/terminal-register.css` has no `box-shadow: none` on any nav-link hover/focus rule | As described in the task brief | Presumably the same two-channel focus-ring mechanism as A8, but unconfirmed against this theme's own history |
| A10 | Font delivery | L4: Google Fonts `Share Tech Mono`, linked and used unrenamed | `themes/terminal/tokens.json:236–237,264–265,268–269`: `'KP Tech Mono', 'Share Tech Mono', ui-monospace, …` — the shipped font file is renamed | Recorded: `themes/terminal/anatomy.md:25–26` — OFL Reserved Font Name "Share" | None known (an asset/licensing gate, not a visual-regression test) |
| A11 | `--panel` / `--card` (minor) | L17: `#0f140f` = hsl(120, 14.3%, 6.9%) | `themes/terminal/tokens.json:18–19`: `hsl(120, 9%, 7%)` = `#101310` (1 unit off per channel) | No reason recorded | None known — negligible, listed for exhaustiveness only |
| A12 | `--void-2` / `--sidebar-background` (minor, footer) | L16: `#070a07` = hsl(120, 17.6%, 3.3%) | `themes/terminal/tokens.json:138–139`: `hsl(120, 12%, 3%)` = `#070907` (1 unit off in green) | No reason recorded | None known — negligible, listed for exhaustiveness only |
| A13 | Headline typing **lead-in delay** | L773–782 (`arrive()`): the first glyph appears after one interval tick (34 ms) — no separate pre-delay | `css/terminal-register.css:41`: `--kp-decipher-lead: 200` (ms), read by `js/effects.js:478` (`later(step, cfg.lead)`) — an extra ~200 ms pause before the first glyph | Not terminal-specific; a shared knob across every "type"/"decipher" theme. No reason recorded in anatomy.md | None known |
| A14 | Headline typing **speed** (minor) | L782: `setInterval(..., 34)` → ≈29.4 chars/s | `css/terminal-register.css:40`: `--kp-decipher-cps: 30` → 33.3 ms/char | No reason recorded | None known — negligible, listed for exhaustiveness only |

---

## Class B — the demo could not show it; the package's own mechanism, appearance carried

| # | Item | What the package does | Where |
|---|---|---|---|
| B1 | Theme applied to the document root | `[data-theme='terminal']` on `<html>`, so the bezel/sweep live on `::before`/`::after` of the root instead of dedicated `<div>`s the demo used | `css/terminal-register.css:74,97`; disclosed in `anatomy.md:87` |
| B2 | `>` prompt in the input not painted | DI9 (a background layer cannot name a literal colour for a glyph) | `anatomy.md:81–83`, `css/terminal-register.css:524–530` |
| B3 | Man page `NAME` heading dropped | KT5 (heading text is copy) | `anatomy.md:83–84` |
| B4 | Caret-in-box mechanism reimplemented | Demo: a separate absolutely-positioned `.caret` span with its own `opacity` blink keyframe. Package: `--kp-col` written onto the input itself by `js/effects.js:706–739`, painted as a `background-image` layer toggled via `background-size` (`css/terminal-register.css:524–530, 1233–1243`). Same 1 Hz visual result, different technique, and consistent with the R6-Q7 decision (cursor lives in the box) | `js/effects.js:699–739`; `css/terminal-register.css:518–530` |
| B5 | Typing-caret mechanism carried structurally identically | `el.append(caret)` per glyph, caret removed on finish — matches the demo's approach closely (`js/effects.js:451–479` vs. demo L769–782) | — |
| B6 | Sweep, tube-off and rule-draw **keyframe values** match the demo exactly | `kp-sweep` (10 s, rest 80%), `kp-tube-off` (420 ms, `steps(4,end)`, the exact same three keyframe stops), `kp-rule-in` (900 ms, `steps(12,end)`) — verified value-for-value against demo L74–79, L508–513, L363–374 | `css/terminal-register.css:1222–1262`, `1191` |
| B7 | Redaction stagger timings match | `--kp-redact-stagger: 200ms`, third redaction at `2 × stagger` = 400 ms, matching demo L476–477 exactly | `css/terminal-register.css:38, 1199–1204` |
| B8 | WCAG contrast figures hold | Recomputed independently (not merely re-read from the demo's own header): phosphor/panel 12.96→13.01, primary/void 12.69→12.69, dim/void 8.36→8.36, accent/void 15.57→15.57, alarm/void 5.43→5.43, alarm/panel 5.21→5.23 — all within rounding of the demo's stated figures (L12) despite the small `--card`/`--secondary` colour drift in A6/A11 | computed for this audit |
| B9 | Hazards refused, honoured | No flicker keyframe, no animated raster, no jitter/noise class exists anywhere in `css/terminal-register.css` — the refusals the demo's header documents (L9) are upheld | verified by full read of the register |
| B10 | Two-channel focus ring | Carried by the package-wide `:focus-visible` rule (`css/_rules.css:371`) plus terminal's own `border-color: var(--primary)` on field focus — matches the demo's DI2 approach, not terminal-specific | — |
| B11 | Wipe-form arm/disarm confirmation ("wipe — y/n?") | Present in the demo (L831–849) and marked required by `showcase/concept-demo.json` (`data-kp-confirm`, line 105–107) — implemented as a package-wide mechanism outside the terminal register's own file, not audited line-by-line here | — |

---

## Class C — the package adds, the demo did not have

| # | Item | Where |
|---|---|---|
| C1 | `.kp-icon-button` styling — no icon button exists in the demo | `css/terminal-register.css:475–485` |
| C2 | Full component coverage beyond the demo's scope: table, datatable, diff, timeline, tree, reorder, tabs, breadcrumb, pagination, wizard, datepicker, colour picker, shortcuts palette, skeleton, spinner, empty state, error text, badges/tags, health dot — none of these appear in "Green Phosphor" at all; they answer the register's general "one answer per component root" obligation (56 of 64 roots, per `anatomy.md:78` and `REALIZATION_PLAN.md:563`) | `css/terminal-register.css:708–1021` |
| C3 | Scrollbar colour (`scrollbar-color`) — not shown or mentioned in the demo | `css/terminal-register.css:1023–1025` |

---

## Notes / uncertain

- **A9** (nav-link `box-shadow: none` removal) is stated in the task brief as an already-known, fixture-forced deviation, but the only prior snapshot available in the scratchpad (`terminal-register.bak`) does not contain the rule described — it may predate that snapshot. Flagging this gap rather than guessing at a line number.
- **A5**'s "what goes red" is a guess (KT8's foreign-colour detector) — I did not run it and could not confirm its exact test file/name without executing the browser suite, which the brief asked me to avoid.
- Did not run any Playwright tests; the "what goes red" column beyond A1/A8 is read from the test source and cross-referenced by hand, not executed.
- `js/effects.js:787` computes the boot-overlay removal fallback timer from `TIMINGS['kp-crt-off']` (550 ms) rather than the register's own `kp-tube-off` (420 ms) keyframe duration. This looks like a latent double-fire risk (the `animationend` listener and the fallback `setTimeout` could both call `remove()`), but it is not a demo-vs-package **value** difference, so it is not in the tables above — flagging it here as a possible separate defect worth a look.
- Did not attempt to independently re-derive or dispute the DI3/DI4/DI5/DI6/DI9 invariant answers in `anatomy.md` — those are Kenny's own prior sign-offs, not demo-vs-package comparisons.
- Class B/C lists are for completeness per the task's method step 5; they are not decisions Kenny needs to make, only Class A is.
